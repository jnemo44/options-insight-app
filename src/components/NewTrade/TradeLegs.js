function TradeLegs (props) {
    return (
        // Need an add leg button
        // Each leg needs...
            // -Price
            // -Buy/Sell Toggle
            // -Strike Selector
            // -Call/Put Toggle
    );
}