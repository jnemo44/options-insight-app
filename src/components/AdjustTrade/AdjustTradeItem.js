

function AdjustTradeItem (props) {
    return (
        <div>Open Date:{props.openDate} Close Date:{props.closeDate} Open Price:{props.openPrice} Close Price:{props.closePrice}</div>
    )
}

export default AdjustTradeItem;